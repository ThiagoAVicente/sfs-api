when:
  event: [push, pull_request]
  branch: [main]

steps:
  - name: test
    image: python:3.13
    commands:
      - curl -LsSf https://astral.sh/uv/install.sh | sh
      - export PATH="/root/.local/bin:$PATH"
      - uv sync --extra dev
      - uv run pytest -v
